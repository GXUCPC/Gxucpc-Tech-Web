/**
 * 后端可用性总开关
 *
 * - false（纯前端模式）：隐藏所有依赖后端接口的元素与按钮，并跳过自动发起的请求：
 *   footer（含 Giscus 留言区）、首页「最新文章」区块、顶部「通知/意见反馈/登录」按钮、
 *   「文章」「赛事报名」导航入口，以及埋点上报和登录态检查。
 * - 后端联调/部署就绪后改回 true，以上内容全部恢复，无需再改动各组件。
 */
export const BACKEND_ENABLED = false
